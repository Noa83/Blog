{% extends 'baseBlog.html.twig' %}

{% block lateral %}
    {{ include('backLateral.html.twig') }}
{% endblock %}

{% block body %}

    <div class="col-md-12">
        {% for type, flash_messages in app.session.flashBag.all %}
            {% for flash_message in flash_messages %}
                <div class="alert-{{ type }} text-center">
                    {{ flash_message }}
                </div>
            {% endfor %}
        {% endfor %}
    </div>


    {{ include(':Creation_Article:formTinyMCEEmpty.html.twig') }}

        <div class="col-sm-12">
            <input type="button" data-toggle="modal" data-target="#myModal" value="Publier"
                   class="btn btn-default pull-right"/>
        </div>
        {{ include(':Creation_Article:modalePublier.html.twig') }}
        <div class="col-sm-12">
            <input id="publish" type="submit" name="submitAction" value="Enregistrer" class="btn btn-primary pull-right"/>
        </div>
    {{ form_end(formView) }}



{% endblock %}

{% block javascripts %}

{% endblock %}
