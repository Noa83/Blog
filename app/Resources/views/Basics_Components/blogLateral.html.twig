    <nav id="menu" class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                        data-target="#menu-navbar-collapse" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>

            <div class="collapse navbar-collapse" id="menu-navbar-collapse">
                <ul class="nav nav-pills nav-stacked">
                    <h3 class="lateral_titles text-center">Chapitres précédents</h3>
                    {% for chapter in chaptersList %}
                    <li><a href="{{ path('visualization_article', {'id': chapter.id}) }}">
                        {{ chapter.title }} <p class="date_chapter pull-right">Créé le: {{ chapter.createdDate|date('d-m-Y') }}</p></a></li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </nav>
