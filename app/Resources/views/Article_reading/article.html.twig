{% extends 'baseBlog.html.twig' %}

{% import ':Article_reading/Macro:recursiveComments.html.twig' as macro %}

{% block lateral %}
    {{ include('blogLateral.html.twig') }}
{% endblock %}

{% block body %}
    <div class="col-sm-12 article_case">
        <div class="col-sm-12">
            <h3 class="col-sm-8">{{ article.title }}</h3>
            <div class="pull-right">Publié le: {{ article.createdDate|date('d-m-Y') }}</div>
        </div>
        <div>{{ article.content|raw }}</div>
        <a href="{{ path('home_page') }}" class="btn btn-info pull-right">
            <i class="glyphicon glyphicon-home"></i>
            Retour à la liste des articles
        </a>
        <a href="#" class="btn btn-success pull-right">
            Chapitre suivant
            <i class="glyphicon glyphicon-chevron-right"></i>
        </a>
        <a href="#" class="btn btn-warning pull-right">
            <i class="glyphicon glyphicon-chevron-left"></i>
            Chapitre précédent
        </a>
        <button class="btn btn-primary pull-right"><i class="glyphicon glyphicon-pencil"></i> Commenter ce chapitre</button>
    </div>
    <div>
        {{ include(':Comments:addComment.html.twig') }}
    </div>
    <div>
        {{ macro.recursiveComments(comments) }}










        {#{% for comment in comments %}#}
            {#<div>#}
                {#<p>{{ comment.author }}</p>#}
                {#<p>Publié le: {{ comment.publishedDate|date('d-m-Y') }}</p>#}
                {#<p>{{ comment.content }}</p>#}
                {#<a href="{{ path('signal', {'id': comment.id}) }}" class="btn btn-warning">signaler ce commentaire</a>#}
            {#</div>#}
        {#{% endfor %}#}
    </div>

{% endblock %}

